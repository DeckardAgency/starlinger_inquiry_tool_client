{
  "buildCommand": "ng build --configuration production",
  "outputDirectory": "dist/starlinger_inquiry_tool_client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
